# CameraNode

Placeholder for a future ROS2 camera adapter.

Keep production camera transport in ROS2. The C++ `Camera` abstraction in `astrall_core` remains useful for simulation, demos, and minimal non-ROS runtime paths.
